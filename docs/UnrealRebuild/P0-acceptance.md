# P0 implementation evidence

P0 is complete and accepted on 2026-09-19. Native checks passed, the owner confirmed the Shipping build works and approved its visuals, and the owner accepted the historical P0 coordinator exception. Sol completed SEC01/SEC02 closeout within the plan's procedural/source-identity and reviewer-tool boundaries. P0 landed at commit `31af9880e12bd8230508a42ca6789d405e5b4bbe`; the TypeScript application, existing saves and preparation sources are preserved. P1 is not authorized by this acceptance.

Effective 2026-09-20, **Sol High is the main coordination and implementation agent, replacing Astra. Astra is reserved for specific owner-approved escalations.** Terra High may perform a separately authorized non-author review or explicitly assigned bounded work, but is not the routine parent. Project-local model settings are requests rather than proof of effective backend routing.

## Executed on 2026-09-18

- Prerequisites: Unreal Engine 5.8.2 (changelist 56702186), Build Tools 2022 17.14.41, MSVC 14.44.35229, Windows SDK 10.0.26100.0, CMake 3.31.6-msvc6 and .NET Framework SDK 4.8. Exact engine/compiler hashes are pinned in `Tools/Build/toolchain-lock.json`.
- `python Tools/Build/p0.py check`: static module/source checks, 71 archived planning-file hashes, 46 preparation-source anchors, repository guidance checks and 19 Python tooling tests passed. The original pack checker separately retained its Windows path-separator failure (82/83); its archive was not altered.
- `python Tools/Build/p0.py domain`: all three native checks, both direct/transitive forbidden-Unreal-header compiler failures, and failed/empty/crashed/timed-out negative controls passed. Latest code-changing run: `20260918T222133.434453Z-7d04f39a`.
- Editor compilation and linking passed: `20260918T222123.054922Z-20902b94`. The editor's modular DLL build exposed an export macro dependency absent from monolithic Game builds; compiler-native DLL visibility fixed it without adding Unreal includes to the pure header. The CMake test still compiles the same implementation.
- The real editor created exactly three owned assets: `Bootstrap.umap`, `M_Bootstrap.uasset`, `WBP_Bootstrap.uasset`. Generation and an unchanged second invocation passed: `20260918T222401.141293Z-b16b2eb5`. Class/hash/intended-property records are in `Content/P0Generated/ownership.json`; property values still need owner inspection.
- The first asset invocation correctly failed its source-change guard when Unreal's default Android file-server plugin appended project settings. Those generated settings were removed, and AndroidFileServer/UdpMessaging/TcpMessaging were explicitly disabled. The successful rerun changed no unowned source.
- Development cook/archive passed: `20260918T222432.644780Z-a750043e`; packaged startup passed: `20260918T222653.784931Z-5095e2dc`.
- Shipping cook/archive passed: `20260918T222721.376980Z-3a191405`; packaged startup passed: `20260918T222903.198163Z-9f432947`.
- Both startup probes loaded the cooked widget, initialized the native game mode and called the domain boundary. Package hashes matched before/after execution. Development used D3D12 on NVIDIA GeForce RTX 3060 Ti, driver 591.86. Successful startup is not visual approval.
- Shipping had no TCP listener or UDP endpoint in 19 process-attributed samples over 20 seconds. See `test-results/p0/shipping-network-audit.json` and its hashed Python script. The initial PowerShell script was blocked by execution policy; Python used ordinary Windows diagnostics without changing security settings. Only the owned test process tree was terminated.
- Project profiles parse and the owner's CLI accepts Sol High configuration. A separate read-only shell probe read source and could not create a file. This is not proof of coordinator model routing or every reviewer tool's effective restrictions. Global Codex settings and credentials were unchanged.
- One Terra High reviewer session (`01a0b6a9-07d4-76f3-9537-2991281024e8`) reviewed a 30-file immutable source bundle after its initial shell reads were denied. The continuation disabled shell, image generation, browser, app/plugin and further-agent features. It found two correctness gaps, both fixed by the coordinator: readiness now checks the exact Bootstrap map package, and the forbidden-header gate requires the actual missing-header diagnostic. A real resolved-header compile-error fixture proves that the second gate rejects the former false pass. Original findings remain in `test-results/p0/review/findings.md`; fixes are not independently re-reviewed.
- That single review session reported 87,062 input tokens (19,712 cached) and 7,518 output tokens across two turns. Of the output, 6,042 were reported as reasoning tokens. Its persisted client metadata confirms `gpt-5.6-terra`, High reasoning, read-only sandbox and approval policy `never` for both turns. These are client records, not independent backend/OS attestation.
- The main thread's recorded cumulative counter reported 25,142,111 input tokens (24,459,776 cached) and 166,710 output tokens. This includes earlier work in this conversation and repeated cached context; it is not a task-isolated or monetary total. The main client metadata records `gpt-6-astra` at xhigh, so repository Sol/High configuration did not change this active thread. See `test-results/p0/main-agent-usage.json` and the reviewer receipt. These snapshots precede the final response.

## Receipts and current-source verification

The invocation IDs above are historical evidence tied to their full source manifests. Subsequent documentation changes alter that identity; consult the latest configuration-specific package/smoke reports for current-source evidence. Each command retains its immutable invocation directory under `test-results/p0/runs/` plus a latest report. Long logs stream to files and survive timeout.

Latest reports: `check.json`, `domain.json`, `build.json`, `assets.json`, `package-Development.json`, `smoke-Development.json`, `package-Shipping.json`, `smoke-Shipping.json`. Review output, when executed, is recorded separately under `test-results/p0/review/`; do not infer review success from this document or the installed role name.

## Closeout and qualification limits

- Sol's bounded closeout found no remaining SEC01/SEC02 blockers. Receipt: `test-results/p0/sec01-sec02-closeout.json`, SHA-256 `19777764BEB3EC17859798A297B79F700D6662601228C624E66518D43CDEA532`. The stable closeout snapshot was `e0e4184e21340e9c7af61238fb00f329607ca93b5a6c55b6dbfa7627a21f7edb`; comparison with the verified Shipping inputs found only documentation/project-model configuration changes. Final acceptance documentation is a subsequent non-runtime change.
- SEC01 passed through serialized writer ownership, closed source/editor writer routes during verification, complete tracked/untracked/binary before/after hashes, immutable invocation manifests and real mutation negative controls. The plan requires detection/denial where practical, not continuous kernel-level attestation.
- SEC02 passed at the actual client/tool boundary: reviewer metadata was read-only/never; initial shell reads were denied; the immutable-source continuation disabled shell, image, app/plugin/browser/editor-MCP and agent routes and executed no tools. This does not claim independent OS/backend attestation.

- Only one bounded Terra High independent review was authorized and executed. The supplied-text review could not independently inspect asset bytes, runtime artifacts, OS isolation or effective backend model routing. No additional reviewer was launched after the fixes.
- Before/after hashing is not a continuous source lock; the procedural freeze and immutable-review evidence above supply the additional context for SEC01. The owner accepted the historical Astra/xhigh coordinator exception for P0 on 2026-09-19. Sol High replaced Astra as the main agent on 2026-09-20; routine Astra use is not authorized going forward.
- Asset guards have real repeat-run evidence and synthetic overwrite/ownership negative controls. Unrelated dirty editor state was not deliberately introduced on this PC; intended property records are not a full independent property readback.
- Owner visual acceptance passed on 2026-09-19. Clean-machine/offline execution and broader GPU qualification have not been performed. No new terrain, simulation or other P1 implementation is included.
- Existing npm/browser suites were not rerun because application runtime code was unchanged. No benchmark or distribution was performed during qualification. The user subsequently staged and committed the accepted P0 tree as `31af9880e12bd8230508a42ca6789d405e5b4bbe`.
